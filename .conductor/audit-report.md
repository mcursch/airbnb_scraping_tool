# Conductor audit — airbnb_scraping_tool

**34 findings** (16 blocker, 14 major, 4 minor)

## 🔴 Currently broken (12)

- **[blocker] settings.database_url does not exist; should be settings.db_url** — `db/__init__.py`
  - Line 7 accesses settings.database_url, but the Settings class in config.py exposes only a db_url property. Every import of db.repo or any module that triggers db/__init__.py at collection time raises AttributeError, blocking at least five test files (test_airbnb_scraper.py, test_db.py, test_history.py, test_repo.py, test_results_table.py) from loading.
  - _Fix:_ Change settings.database_url to settings.db_url in db/__init__.py line 7.
- **[blocker] Root db/models.py missing get_engine and db/repo.py missing several expected functions** — `db/models.py`
  - test_detail_panel.py imports get_engine from db.models and get_latest_snapshot, get_listing, get_listing_with_latest_snapshot, insert_snapshot from db.repo. None of these exist in the root-level db/models.py or db/repo.py (which only has create_search_run, close_search_run, record_run_stats, upsert_listing, create_listing_snapshot). This causes an ImportError at collection time (masked by the database_url bug but confirmed by source inspection).
  - _Fix:_ Add get_engine to db/models.py and implement the missing repo helpers (get_listing, get_latest_snapshot, get_listing_with_latest_snapshot, insert_snapshot) in db/repo.py.
- **[blocker] batch.py imports SYSTEM_PROMPT, _get_default_client, extract_listings from a stub extractor that does not define them** — `extraction/batch.py`
  - Line 46 does 'from extraction.extractor import SYSTEM_PROMPT, _get_default_client, extract_listings'. The root-level extraction/extractor.py is only a placeholder Protocol file containing ExtractionResult and Extractor; none of those three symbols exist there. This causes an ImportError at collection time, blocking test_batch.py entirely.
  - _Fix:_ Either implement the real extraction pipeline in extraction/extractor.py (matching the full implementation already present in airbnb_scraping_tool/extraction/extractor.py) or update batch.py to import from airbnb_scraping_tool.extraction.extractor.
- **[blocker] pipeline.py lacks the Pipeline class and process_raw_scrape function that multiple files import** — `pipeline.py`
  - cli.py line 77 does 'from pipeline import Pipeline'; test_dedup.py line 25 does 'from pipeline import process_raw_scrape'; test_pipeline.py patches pl_mod.SessionLocal (also absent). pipeline.py only contains run_acquire(). This blocks test_dedup.py from collecting, causes 5 runtime failures in test_pipeline.py (AttributeError on SessionLocal), 5 failures in test_logging_retry_cancel.py (ImportError on Pipeline), and makes the CLI scan command crash at runtime.
  - _Fix:_ Implement the Pipeline class and process_raw_scrape function in pipeline.py as specified; add SessionLocal import.
- **[blocker] Root schemas/__init__.py exports only 2 of 8 required symbols** — `schemas/__init__.py`
  - Only ExtractedListing and ListingExtraction are exported. test_schemas.py imports ExtractionLog, Listing, ListingSnapshot, RawPayload, SearchQuery, and SearchRun from schemas; all six exist in schemas/models.py but are not re-exported, causing an ImportError at collection time.
  - _Fix:_ Add 'from schemas.models import ExtractionLog, Listing, ListingSnapshot, RawPayload, SearchQuery, SearchRun' to schemas/__init__.py and include them in __all__.
- **[blocker] Imports RawPayload from scrapers.base where it is not defined** — `scrapers/airbnb.py`
  - Line 28 does 'from scrapers.base import RawPayload, ScrapeProvider'. scrapers/base.py defines RawScrape, SearchQuery, BlockedError, and ScrapeProvider but not RawPayload. RawPayload lives in schemas/models.py. This ImportError prevents test_airbnb.py and test_fallback_api.py from collecting.
  - _Fix:_ Change the import to 'from schemas.models import RawPayload' (or the appropriate schemas module) in scrapers/airbnb.py.
- **[major] Settings class missing log_dir field required by test_logging_retry_cancel.py** — `config.py`
  - test_logging_retry_cancel.py reads and writes config.settings.log_dir (lines 84, 85, 97, 128, 129, 133). The Settings class in config.py has no log_dir field. At runtime this raises AttributeError: 'Settings' object has no attribute 'log_dir', confirmed by the test runner output for TestDryRun::test_dry_run_cli_exits_0.
  - _Fix:_ Add 'log_dir: str = "logs"' (or similar) to the Settings class in config.py.
- **[major] Root ExtractionLog missing listing_id column and cache_read_tokens column name conflicts with tests and batch.py** — `db/models.py`
  - test_dedup.py accesses log.listing_id on ExtractionLog rows (lines 119, 125, 126, 213), but the root db/models.py ExtractionLog has no listing_id column. Additionally, extraction/batch.py creates ExtractionLog with keyword argument cache_read_input_tokens, while the column is named cache_read_tokens; test_extractor.py also asserts log.cache_read_input_tokens. SQLAlchemy will raise or silently drop the unknown kwarg depending on version.
  - _Fix:_ Add listing_id as a nullable ForeignKey column to ExtractionLog in db/models.py and standardise the cache_read column to either cache_read_tokens or cache_read_input_tokens consistently across db/models.py, extraction/batch.py, and the tests.
- **[major] pretrim fails to detect listings in stays_search_response.json and fails 30% compression threshold on three small fixtures** — `extraction/pretrim.py`
  - _is_listing_like() checks only one dict level deep for price signals, missing the real Airbnb StaysSearch format where the price lives at pricingQuote.structuredStayDisplayPrice.primaryLine.price (four levels down). test_pretrim_contains_price[stays_search_response.json] fails. Additionally, airbnb_page1.json compresses to only 43%, airbnb_page2.json to 38%, and sample_airbnb.json to 67%, all above the 30% limit asserted by test_pretrim_within_30_percent.
  - _Fix:_ Extend _is_listing_like() to look two or three levels deep for price signals, or add a complementary check for the pricingQuote key pattern. For the compression failures, increase the noise-key list or strip more verbose string fields from small fixtures.
- **[major] anthropic and pydantic-settings are direct runtime dependencies not declared in pyproject.toml** — `pyproject.toml`
  - config.py imports pydantic_settings.BaseSettings and extraction/batch.py plus cli.py import anthropic. Neither package is listed under [project] dependencies. They happen to be installed in the current environment as transitive deps of other tools, but a fresh uv sync from pyproject.toml alone would not install them, breaking the app on a clean checkout.
  - _Fix:_ Add 'anthropic>=0.100' and 'pydantic-settings>=2.0' to the [project] dependencies list in pyproject.toml.
- **[major] Root SearchQuery uses field name area_query instead of area, and Source Literal excludes 'hotels'** — `schemas/models.py`
  - test_base.py creates SearchQuery(area='...') and expects sources to default to {'airbnb', 'hotels'}. The root schemas/models.py defines the field as area_query (with min_length=1) and Source as Literal['airbnb','booking','other'] defaulting to ['airbnb']. Three TestSearchQuery tests fail with Pydantic ValidationError: 'area_query field required'. The airbnb_scraping_tool subpackage uses area, making this inconsistency clear.
  - _Fix:_ Rename the area_query field to area in schemas/models.py SearchQuery (or vice-versa — pick one canonical name and use it everywhere), and reconcile the Source Literal and default sources list.
- **[major] AirbnbScraper.search() is async and returns AsyncGenerator, violating the sync ScrapeProvider ABC** — `scrapers/airbnb.py`
  - ScrapeProvider.search() in scrapers/base.py is declared as a synchronous abstract method returning list[RawScrape]. AirbnbScraper.search() is declared async and returns AsyncGenerator[tuple[str, RawPayload], None]. The class does not satisfy the abstract contract and cannot be used anywhere a ScrapeProvider is expected.
  - _Fix:_ Either make AirbnbScraper.search() synchronous (wrapping the async browser call with asyncio.run()) or change the ScrapeProvider ABC to an async interface and update all callers accordingly.

## 🧩 Unfinished / needs implementing (12)

- **[blocker] `settings.database_url` attribute does not exist — should be `settings.db_url`** — `db/__init__.py`
  - `db/__init__.py` calls `create_engine(settings.database_url, ...)` but `config.py` exposes the URL as a `db_url` property, not `database_url`. This raises an `AttributeError` the moment any module imports from `db`, breaking 13 test files at collection time (test_airbnb_scraper, test_db, test_history, test_repo, test_results_table, and others).
  - _Fix:_ Change `db/__init__.py` to use `settings.db_url` to match the `config.Settings` property name, or rename the property in `config.py` to `database_url` and update all references.
- **[blocker] Root-level `extraction/extractor.py` is only a Protocol stub — `batch.py` imports three functions that don't exist there** — `extraction/extractor.py`
  - `extraction/batch.py` line 46 imports `SYSTEM_PROMPT`, `_get_default_client`, and `extract_listings` from `extraction.extractor`. The root-level `extraction/extractor.py` contains only an `Extractor` Protocol and `ExtractionResult` dataclass — none of those three symbols exist. The real `Extractor` class is in `airbnb_scraping_tool/extraction/extractor.py`. The entire batch extraction path is broken at import.
  - _Fix:_ Move or consolidate the full `Extractor` implementation into the root-level `extraction/extractor.py`, adding the `SYSTEM_PROMPT` constant, `extract_listings()` function, and `_get_default_client()` helper that `batch.py` depends on.
- **[blocker] `Pipeline` class and `run_search`/`PipelineResult` symbols are absent** — `pipeline.py`
  - `pipeline.py` implements only a bare `run_acquire()` function. `cli.py scan`, `dashboard/pages/search.py`, `tests/test_pipeline.py`, and `tests/test_logging_retry_cancel.py` all import `Pipeline`, `PipelineResult`, and/or `run_search` from this module; every one of them fails at import. The CLI `scan` command and the dashboard search page are completely non-functional.
  - _Fix:_ Implement a `Pipeline` class with a `run(query, *, dry_run=False, no_extract=False)` method that orchestrates acquire → dedup → extract → store, sets run stats, and commits the `SearchRun`. Also add `PipelineResult` dataclass and `run_search` helper expected by the dashboard.
- **[blocker] Airbnb scraper fails to import due to missing `RawPayload` and six undefined constants** — `scrapers/airbnb.py`
  - The module imports `RawPayload` from `scrapers.base` (not defined there; it lives in `schemas.models` or `airbnb_scraping_tool/schemas/scrape.py`) and imports `AIRBNB_ENDPOINT_PATTERNS`, `AIRBNB_SEARCH_PATH`, `DEFAULT_EXTRA_WAIT_MAX`, `DEFAULT_EXTRA_WAIT_MIN`, `DEFAULT_HEADLESS`, `DEFAULT_PAGE_TIMEOUT_MS`, and `DEFAULT_USER_AGENT` from `scrapers.constants`, none of which are defined in that file. Confirmed: `test_airbnb.py` and `test_fallback_api.py` both crash at collection with `ImportError`.
  - _Fix:_ Add the missing constants to `scrapers/constants.py` and export `RawPayload` from `scrapers/base.py` (or update the import to `schemas.models`). Align constant names with the existing `STAYS_SEARCH_URL_PATTERN`, `REQUEST_DELAY_MIN_S`, etc. already defined in `constants.py`.
- **[blocker] `AirbnbScraper.search()` is `async` returning `AsyncGenerator` — violates the `ScrapeProvider` ABC** — `scrapers/airbnb.py`
  - `ScrapeProvider.search()` in `scrapers/base.py` is declared synchronous, returning `list[RawScrape]`. `AirbnbScraper.search()` is `async def` returning `AsyncGenerator[tuple[str, RawPayload], None]`. Python does not raise at definition time, but the pipeline's synchronous `provider.search(query)` call will return a coroutine object rather than a list, silently producing no results.
  - _Fix:_ Refactor `AirbnbScraper.search()` to be synchronous (wrapping the async browser run with `asyncio.run()`), returning `list[RawScrape]` to match the ABC contract, or make `ScrapeProvider.search()` async and propagate that change through the pipeline.
- **[major] Stage 6 pipeline features entirely absent: structured logging, `--dry-run`, SIGINT cancellation, `log_dir` config** — `-`
  - `config.py` has no `log_dir` field; `Pipeline.run()` (which doesn't exist yet) has no `dry_run` or `no_extract` parameters; there is no SIGINT handler. Seven tests in `test_logging_retry_cancel.py` all fail because `Pipeline` cannot be imported at all, and the CLI `scan` command lacks a `--dry-run` flag. All Stage 6 exit criteria are unmet.
  - _Fix:_ Add `log_dir: str = 'logs'` to `config.Settings`, add `dry_run` and `no_extract` keyword arguments to `Pipeline.run()`, write per-run JSON-lines log files to `log_dir/<run_id>.jsonl`, and install a `SIGINT` signal handler that sets a cancellation flag and marks the run as `'cancelled'`.
- **[major] Results page is a placeholder stub — no listing table, map, charts, or CSV export** — `dashboard/pages/results.py`
  - The Results page renders only a warning: 'Full listing display is implemented in Stage 5. Check back once the persistence and extraction stages are complete.' The page returns immediately without loading any data from the database. Per the spec, Stage 5 should show a sortable/filterable listings table, map view, price histogram, source-comparison chart, and CSV export.
  - _Fix:_ Implement the Results page using the `load_run_df`/`filter_df` functions from `dashboard/components/results_table.py`, the map component from `dashboard/components/map_view.py`, and the chart components from `dashboard/components/charts.py` — all of which are already partially implemented.
- **[major] Three query functions used by the dashboard and tests do not exist in `db/repo.py`** — `db/repo.py`
  - `dashboard/pages/history.py` calls `list_search_runs(limit=50)`, `dashboard/components/results_table.py` calls `repo.get_listings_for_run(run_id, engine)`, and `tests/test_history.py` imports `get_all_runs_with_cost`. None of these functions exist in either the root `db/repo.py` or `airbnb_scraping_tool/db/repo.py`. The History page and Results table component are both non-functional at runtime.
  - _Fix:_ Add `list_search_runs(limit)`, `get_listings_for_run(run_id, engine) -> pd.DataFrame`, and `get_all_runs_with_cost(session)` to `db/repo.py`, querying `search_runs`, `listings`, `listing_snapshots`, and `extraction_logs` as needed.
- **[major] `pretrim()` fails its own fixture-based tests — real Airbnb payloads not reduced below 30% and key fields stripped** — `extraction/pretrim.py`
  - `test_pretrim.py` shows five failures: `airbnb_page1.json`, `airbnb_page2.json`, and `sample_airbnb.json` are not reduced to ≤30% of input size; `stays_search_response.json` has its price and location signals stripped because Airbnb's actual keys (`avgRatingA11yLabel`, `roomType`, `city`) don't match the heuristic's price/lat/name patterns, causing `_is_listing_like()` to return `False` and the fallback filter to discard them.
  - _Fix:_ Expand `_is_listing_like()` to recognise Airbnb-specific field names (e.g. `avgRating`, `city`, `roomType`) as name/location signals, and tune `_slim_dict()` to achieve the target reduction ratio on the real saved fixtures.
- **[major] `FallbackApiProvider.search()` raises `NotImplementedError` unconditionally** — `scrapers/fallback_api.py`
  - The method body consists solely of `raise NotImplementedError('FallbackApiProvider.search() will be implemented in Stage 2')`. Any pipeline run that encounters a block and has a fallback key configured will crash rather than retrying via the paid API.
  - _Fix:_ Implement the HTTP call to the configured scraping-API provider (ScraperAPI or Apify), building the request URL from the provider's documented endpoint format and returning a list of `RawScrape` objects.
- **[minor] Three incompatible `SearchQuery` Pydantic models coexist with conflicting field names** — `-`
  - The codebase has three separate `SearchQuery` classes: `scrapers/base.py` (`area`, `page_limit`), `schemas/models.py` (`area_query`, no `page_limit`), and `airbnb_scraping_tool/schemas/scrape.py` (`area`, no `page_limit`). `scrapers/booking.py` calls `query.page_limit` on the `scrapers.base.SearchQuery`; `test_base.py` instantiates `schemas.models.SearchQuery` and asserts on `guests`/`sources` defaults that differ from the `scrapers.base` version, causing two test failures.
  - _Fix:_ Consolidate to a single canonical `SearchQuery` in `airbnb_scraping_tool/schemas/scrape.py` (or `schemas/models.py`) and update all scrapers and tests to import from that one location.
- **[minor] `ExtractionLog` not exported from `schemas/__init__.py`** — `schemas/__init__.py`
  - `ExtractionLog` is defined in `schemas/models.py` but `schemas/__init__.py` only re-exports `ExtractedListing` and `ListingExtraction`. `tests/test_schemas.py` imports `ExtractionLog` from `schemas` and fails at collection with `ImportError`.
  - _Fix:_ Add `from schemas.models import ExtractionLog` and include `'ExtractionLog'` in `__all__` inside `schemas/__init__.py`.

## 🐛 Likely bugs & security (10)

- **[blocker] `settings.database_url` attribute does not exist — should be `settings.db_url`** — `db/__init__.py`
  - `db/__init__.py` line 7 accesses `settings.database_url`, but the `Settings` class in `config.py` exposes the connection string as the property `db_url`, not `database_url`. This causes `AttributeError: 'Settings' object has no attribute 'database_url'` on every module-level import of `db.models`, breaking all tests in `test_repo.py`, `test_extractor.py`, `test_dedup.py`, `test_db.py`, `test_batch.py`, and `test_detail_panel.py`. Confirmed by running `pytest`.
  - _Fix:_ Change `settings.database_url` to `settings.db_url` in `db/__init__.py`.
- **[blocker] Batch module imports `SYSTEM_PROMPT`, `_get_default_client`, `extract_listings` from a stub that defines none of them** — `extraction/batch.py`
  - `extraction/batch.py` line 46 imports `SYSTEM_PROMPT`, `_get_default_client`, and `extract_listings` from `extraction.extractor`, but the flat `extraction/extractor.py` is a stub that only defines the `Extractor` Protocol and `ExtractionResult` dataclass. None of the three imported names exist. The full implementation lives in `airbnb_scraping_tool/extraction/extractor.py`, a completely separate module. Any call to `batch_extract` will raise `ImportError`.
  - _Fix:_ Either implement `SYSTEM_PROMPT`, `_get_default_client`, and `extract_listings` in the flat `extraction/extractor.py`, or update `batch.py` to import from `airbnb_scraping_tool.extraction.extractor` and ensure that package is on the path.
- **[blocker] `Pipeline` class and `process_raw_scrape` function are absent from `pipeline.py`** — `pipeline.py`
  - `cli.py` does `from pipeline import Pipeline`, `tests/test_pipeline.py` patches `pl_mod.SessionLocal` expecting a `Pipeline` class, and `tests/test_dedup.py` imports `process_raw_scrape`. The only public symbol in `pipeline.py` is the `run_acquire` function; neither `Pipeline` nor `process_raw_scrape` exists. Running `python -c 'from pipeline import Pipeline'` confirms `ImportError`, meaning the `scan` CLI command fails at startup.
  - _Fix:_ Implement the `Pipeline` orchestrator class (and `process_raw_scrape`) in `pipeline.py` to wire `run_acquire` → extraction → repo store, as the tests expect.
- **[blocker] ImportError: `RawPayload` and multiple constants imported from wrong modules** — `scrapers/airbnb.py`
  - `scrapers/airbnb.py` imports `RawPayload` from `scrapers.base` and `AIRBNB_ENDPOINT_PATTERNS`, `AIRBNB_SEARCH_PATH`, `DEFAULT_EXTRA_WAIT_MAX`, `DEFAULT_EXTRA_WAIT_MIN`, `DEFAULT_HEADLESS`, `DEFAULT_PAGE_TIMEOUT_MS`, and `DEFAULT_USER_AGENT` from `scrapers.constants`. None of these names exist in those modules (`scrapers.base` defines `RawScrape`, not `RawPayload`; none of the seven constant names appear in `scrapers/constants.py`). Confirmed with `python -c 'from scrapers.airbnb import AirbnbScraper'` → `ImportError`.
  - _Fix:_ Replace the `RawPayload` import with `RawScrape` and add the missing constants (`AIRBNB_ENDPOINT_PATTERNS`, `AIRBNB_SEARCH_PATH`, etc.) to `scrapers/constants.py`, or update the import to use the actual constant names already defined there.
- **[blocker] `_persist()` imports `RawScrapeRow` from `db.models`, but the class is named `RawScrape`** — `scrapers/booking.py`
  - `BookingScraper._persist()` at line 347 does `from db.models import RawScrapeRow`, but `db/models.py` has no such class — the ORM model for raw scrapes is named `RawScrape`. Any code path that calls `_persist` with a non-None session will hit an `ImportError` at runtime. The error is silently swallowed by the broad `except Exception` on line 360, causing persistence to silently fail without surfacing the problem.
  - _Fix:_ Change `RawScrapeRow` to `RawScrape` on the import and constructor in `_persist()`. Also consider logging the exception at ERROR level rather than swallowing it, since silent persistence failure defeats the crash-safety guarantee.
- **[major] `ExtractionLog` is constructed with `cache_read_input_tokens` but the DB column is `cache_read_tokens`** — `extraction/batch.py`
  - At lines 208–210, `batch.py` creates `ExtractionLog(..., cache_read_input_tokens=...)`. The ORM model in `db/models.py` defines the column as `cache_read_tokens` (confirmed by grep). SQLAlchemy will ignore the unrecognised keyword argument silently (or raise a `TypeError` depending on the SQLAlchemy version), so prompt-cache read tokens are never persisted for batch runs.
  - _Fix:_ Change the keyword argument in `batch.py` from `cache_read_input_tokens` to `cache_read_tokens` to match the `ExtractionLog` ORM column definition.
- **[major] `SearchQuery` declares `area_query` as the required field, but every caller passes `area=`** — `schemas/models.py`
  - `schemas/models.py` defines `SearchQuery` with the required field `area_query`. `tests/test_base.py` constructs `SearchQuery(area='Paris, France')` (three tests), and `pipeline.py`/`cli.py` use the `schemas.SearchQuery` with `area=`. Pydantic raises a validation error because `area_query` is missing. All three `TestSearchQuery` tests fail as confirmed by running `pytest tests/test_base.py`.
  - _Fix:_ Rename the field in `schemas/models.py` from `area_query` to `area`, or update all callers to pass `area_query=`. The field is already named `area` in `scrapers/base.py`'s `SearchQuery` and in `airbnb_scraping_tool/schemas/scrape.py`, so aligning them removes the confusion.
- **[major] `AirbnbScraper.search()` is `async def` returning `AsyncGenerator`, violating the synchronous `ScrapeProvider` interface** — `scrapers/airbnb.py`
  - The `ScrapeProvider` ABC declares `search(self, query: SearchQuery) -> list[RawScrape]` as a synchronous method. `AirbnbScraper.search()` is declared `async def` and uses `yield`, making it an async generator. Calling it synchronously (as `run_acquire` does) returns a coroutine/async-generator object, not a list, so `results.extend(raw)` will fail at runtime with a `TypeError`.
  - _Fix:_ Either wrap the async browser logic in `asyncio.run()` inside a synchronous `search()` method so the public interface remains synchronous, or change `ScrapeProvider` to an async ABC — but note that would require updating all other scrapers and callers.
- **[minor] `upsert_listing` update path silently preserves stale values — can never clear a field to `None`** — `db/repo.py`
  - In `upsert_listing`, the update branch loops `if incoming is not None: setattr(existing, field, incoming)`. Any field that previously held a value can never be set back to `None` by a subsequent scrape (e.g., a listing that loses its rating or removes amenities). The stale value is silently retained, producing incorrect data for downstream consumers.
  - _Fix:_ Remove the `if incoming is not None` guard, or only skip update when the incoming value equals the existing value (`if getattr(existing, field) != incoming`), so that an explicitly `None` value from the new scrape is written through.
- **[minor] `_fetch_playwright()` hard-codes HTTP status 200, bypassing status-code block detection** — `scrapers/booking.py`
  - After the Playwright path renders the page, `_fetch_playwright()` calls `self._check_for_block(200, html, url=url)` with a hardcoded `200`. If Booking.com returns a real 403, 429, or 503 response, the status-code branch of `_check_for_block` (which compares against `BLOCKED_STATUS_CODES`) never fires. The scraper will only detect a block if the body happens to contain one of the CAPTCHA text patterns.
  - _Fix:_ Capture the real HTTP status from the Playwright response object (`resp = page.goto(url, ...)` returns a `Response`; use `resp.status` if non-None) and pass it to `_check_for_block` instead of the hardcoded `200`.

